[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    scale_s2_medium: S2 medium volume ingest benchmark (slow, opt-in)
    scale_s2_large: S2 large volume ingest benchmark (very slow, opt-in)
